{% set secondary_items = menu('secondary') %}
{% if secondary_items is not empty %}
<nav class="stamp-cluster" aria-label="Secondary">
    <p class="stamp-cluster-kicker" aria-hidden="true">CONTENTS</p>
    <ul class="stamp-cluster-list">
        {% for item in secondary_items %}
            <li class="stamp-cluster-item{% if item.children is not empty %} has-children{% endif %}">
                {% if item.target_type == 'label' %}
                    <span class="stamp-cluster-label">{{ item.label }}</span>
                {% else %}
                    <a href="{{ item.url }}" class="stamp-cluster-link" {% if item.open_in_new_tab %}target="_blank" rel="noopener"{% endif %}>{{ item.label }}</a>
                {% endif %}
                {% if item.children is not empty %}
                    <ul class="stamp-cluster-children">
                        {% for child in item.children %}
                            <li>
                                {% if child.target_type == 'label' %}
                                    <span class="stamp-cluster-label">{{ child.label }}</span>
                                {% else %}
                                    <a href="{{ child.url }}" class="stamp-cluster-link" {% if child.open_in_new_tab %}target="_blank" rel="noopener"{% endif %}>{{ child.label }}</a>
                                {% endif %}
                            </li>
                        {% endfor %}
                    </ul>
                {% endif %}
            </li>
        {% endfor %}
    </ul>
</nav>
{% endif %}
