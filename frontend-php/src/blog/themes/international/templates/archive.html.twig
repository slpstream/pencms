{{ theme.partial('head') | raw }}
{{ theme.partial('header') | raw }}

<main id="main" class="site-main page-archive">
    <section class="wrap archive-container">
        <p class="section-kicker">Archive</p>
        <h1 class="archive-headline">{{ (hero_title | default(category | default('Articles'))) | title }}</h1>
        <div class="post-divider" aria-hidden="true"></div>

        {% set post_list = (dossiers is defined and dossiers is not empty) ? dossiers : ((posts is defined and posts is not empty) ? posts : []) %}
        {% if post_list is not empty %}
            <ol class="archive-list">
                {% for dossier in post_list %}
                    {% set url = contentUrl(dossier) %}
                    <li class="archive-item">
                        <span class="archive-date">
                            {% if dossier.date is not empty %}
                                <time datetime="{{ dossier.date }}">{{ dossier.date | date('Y.m.d') }}</time>
                            {% else %}
                                —
                            {% endif %}
                        </span>
                        <a href="{{ url }}" class="archive-title">{{ dossier.hero_title | default(dossier.title) }}</a>
                    </li>
                {% endfor %}
            </ol>
        {% else %}
            <p class="empty-feed">Nothing in this archive yet.</p>
        {% endif %}
    </section>
</main>

{{ theme.partial('footer') | raw }}
